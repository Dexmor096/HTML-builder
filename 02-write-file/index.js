const { stdin, stdout } = process;
const fs = require('fs');
const path = require('path');


// const writeStream = fs.createWriteStream(path.join(__dirname, 'text.txt'));
// const message = stdout.write('Привет, как делишки? напиши  что-нибудь:');
// writeStream.on('data', () => {
//   console.log(message);
// });
// message.on('data', chunk => output.write(chunk));

